{
  "name": "thermalsight",
  "version": "1.2.6",
  "description": "Thermal Analysis Student Explorer",
  "author": "Aditya",
  "repository": "https://github.com/Aditya42069/ThermalSight-App",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "node ./node_modules/vite/bin/vite.js build",
    "dist": "npm run build && electron-builder"
  },
  "build": {
    "appId": "com.edu.thermalsight",
    "directories": {
      "output": "release"
    },
    "extraResources": [
      {
        "from": "../backend/dist/analyzer.exe",
        "to": "backend/analyzer.exe",
        "filter": ["**/*"]
      },
      {
        "from": "../backend/dist/analyzer",
        "to": "backend/analyzer",
        "filter": ["**/*"]
      }
    ],
    "mac": { "target": "dmg" },
    "win": { "target": "nsis" },
    "linux": { "target": "AppImage" }
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "concurrently": "^8.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.13.3",
    "vite": "^8.0.12",
    "wait-on": "^7.0.1"
  }
}
